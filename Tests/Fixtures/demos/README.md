# Demo recordings

These screen recordings must be captured on a Mac **with an attached display**
(the build/verification session was headless — `screencapture` returns
"could not create image from display").

- `m1-offline.mov` — M1: summon → streamed cited answer, network off (BS-M1).
- `beats-siri.mov` — M12/BS-M12: the full continuous demo — summon →
  personal+grounded answer with verified citations → open source → inspector
  delete → re-ask, watch it forget → egress indicator at zero throughout.

The headless-captured transcript of BS-M12 (via `mnemoctl`, egress measured 0)
is in `scripts/m12-acceptance.md`.
