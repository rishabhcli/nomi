# D-0781 Evidence — Ingestion: property-based invariants

**Seed:** `ddf40571e3b2`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0781IngestionTests
```

## Failure modes fixed (file:line)

1. `Phase2Techniques.swift — deterministic technique hardening added`
2. `NotchReducer.swift:175 — terminal states always render messages`
3. `GroundingCheck.swift:5 — single citation-integrity implementation`

## Linux CI attempt (Swift 6.2)

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test --filter D0781IngestionTests` required for PASS/FAIL.

## Contract delta

- Phase2Techniques hardening for `property-based invariants` on `Ingestion`
