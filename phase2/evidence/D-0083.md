# D-0083 AdaptiveEffort: char-span fuzzing

**Seed:** `88260ee984c9`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AdaptiveEffort.swift:30` | Out-of-range span indices crashed resolver | Bounds-checked span fuzz path |
| 2 | `CharSpan.swift:40` | Single-token false positives | Minimum token length guard |
| 3 | `AdaptiveEffort.swift:55` | Non-deterministic fuzz ordering | Seed-stable fuzz via Phase2RNG |

## Tests

`Tests/MnemoOrchestratorTests/D0083AdaptiveEffortTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0083AdaptiveEffort
```

**Note:** `swift test` requires macOS (AVFoundation).
