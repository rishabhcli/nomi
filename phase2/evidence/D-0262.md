# D-0262 CitationVerifier: concurrency stress under WorkScheduler

**Seed:** `d9c31ac8a0cb`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `WorkScheduler.swift:72` | Interactive token leak | endInteractive removes token |
| 2 | `WorkScheduler.swift:81` | shouldBackgroundYield stale | Actor-isolated counter |
| 3 | `IngestGate.swift` | Background blocked interactive | Yield at chunk boundary |

## Tests

`Tests/MnemoOrchestratorTests/D0262CitationVerifierTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0262CitationVerifier
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
