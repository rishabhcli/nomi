# D-0005 EngineClient: cache poisoning resistance

**Seed:** `2749726a9b94`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `EngineClient.swift:102` | Remote `filepath`/`metadata` URLs accepted into `SourceLocator` | `sanitizeEnginePath` strips `http(s)://` paths |
| 2 | `EngineClient.swift:123` | Poisoned wire rows merged into search results | `isPoisonedWireResult` filter before `mapWireResult` |
| 3 | `EngineClient.swift:66-70` | No loopback validation on `baseURL` | `preconditionFailure` if host not loopback per `EgressGuard` |
| 4 | `EngineClient.swift:99` | Unstable wire identity enabled cache key collisions | `wireResultIdentity` stable `docId|text` key |

## Tests

`Tests/MnemoOrchestratorTests/D0005EngineClientTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0005EngineClient
```

**Note:** `swift test` requires macOS (AVFoundation).
