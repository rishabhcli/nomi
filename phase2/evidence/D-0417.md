# D-0417 CharSpan: AsyncStream cancellation

**Seed:** `847c6ad760ae`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `QueryService.swift` | Stream not cancelled on new query | Cancellation token wired |
| 2 | `Router.swift` | Escalation stream survives cancel | Task.isCancelled check |
| 3 | `AgenticGrep.swift:147` | Cancelled grep continued hopping | Early return on cancel |

## Tests

`Tests/MnemoOrchestratorTests/D0417CharSpanTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0417CharSpan
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
