# D-0065 Prompt: cache poisoning resistance

**Seed:** `e57a9cb7a959`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `Prompt.swift:25` | Remote URL in wire result accepted | Poisoned path/host stripped |
| 2 | `EngineClient.swift:50` | Non-loopback backing store leaked | Loopback-only validation |
| 3 | `Prompt.swift:70` | Cache key included poisoned host | Sanitized cache key |

## Tests

`Tests/MnemoOrchestratorTests/D0065PromptTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0065Prompt
```

**Note:** `swift test` requires macOS (AVFoundation).
