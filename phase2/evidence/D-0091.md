# D-0091 FollowUp: agentic grep deadlock prevention

**Seed:** `d78f6a9521d8`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:153` | Repeated hop spun forever | `isRepeatedHop` stops loop |
| 2 | `FollowUp.swift:40` | Identical query re-queued | Hop deduplication set |
| 3 | `FollowUp.swift:90` | Max hops unenforced | Hard cap on hop count |

## Tests

`Tests/MnemoOrchestratorTests/D0091FollowUpTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0091FollowUp
```

**Note:** `swift test` requires macOS (AVFoundation).
