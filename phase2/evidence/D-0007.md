# D-0007 CitationVerifier: router escalation boundaries

**Seed:** `0f386ab3d7a5`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `CitationVerifier.swift:147` | `claim.count < 3` auto-supported — punctuation fragments skewed escalation | `isTrivialFragment` alphanumeric check |
| 2 | `CitationVerifier.swift:188-191` | Partial grounding had no router escalation signal | `needsRouterEscalation` + `routerEscalationEvents` |
| 3 | `CitationVerifier.swift:189` | `allUnsupported` used raw text length, not stripped claim | Uses `isTrivialFragment` on stripped citations |

## Tests

`Tests/MnemoOrchestratorTests/D0007CitationVerifierTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0007CitationVerifier
```

**Note:** `swift test` requires macOS (AVFoundation).
