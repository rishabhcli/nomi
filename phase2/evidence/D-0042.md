# D-0042 Provenance: concurrency stress under WorkScheduler

**Seed:** `5c2083671b10`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `Provenance.swift:70` | Interactive token leak under concurrent stress | WorkScheduler token lifecycle hardened |
| 2 | `WorkScheduler.swift:93` | Background chunks not abandoned on yield | Verified `runBackgroundChunked` abandons |
| 3 | `Provenance.swift:45` | Race on shared mutable state | Actor-isolated access paths |

## Tests

`Tests/MnemoOrchestratorTests/D0042ProvenanceTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0042Provenance
```

**Note:** `swift test` requires macOS (AVFoundation).
