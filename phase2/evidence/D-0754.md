# D-0754 Evidence — FollowUp: answer cache key collision

**Seed:** `e0b49d67b2c6`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0754FollowUpTests
```

## Failure modes fixed (file:line)

1. `AnswerCache.swift:65 — key() ignored corpusVersion normalization → Phase2Techniques.cacheKey`
2. `FollowUp.swift:72 — suggest deduped by normalized query key`
3. `Phase2Techniques.swift:40 — whitespace/diacritic-normalized cache keys`

## Linux CI attempt (Swift 6.2)

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test --filter D0754FollowUpTests` required for PASS/FAIL.

## Contract delta

- Phase2Techniques hardening for `answer cache key collision` on `FollowUp`
