# D-0231 WorkScheduler: agentic grep deadlock prevention

**Seed:** `cc1b45b6f73c`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:153` | Repeated hop spun forever | `isRepeatedHop` stops loop |
| 2 | `WorkScheduler.swift:40` | Identical query re-queued | Hop deduplication set |
| 3 | `WorkScheduler.swift:90` | Max hops unenforced | Hard cap on hop count |

## Tests

`Tests/MnemoOrchestratorTests/D0231WorkSchedulerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0231WorkScheduler
```

**Note:** `swift test` requires macOS (AVFoundation).
