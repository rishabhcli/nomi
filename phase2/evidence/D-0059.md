# D-0059 SpanResolver: QueryEvent ordering guarantees

**Seed:** `95fe64077e63`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `SpanResolver.swift:80` | Sources after tokens | Sources precede tokens invariant |
| 2 | `QueryService.swift:200` | Done not last | Stream ends with `.done` |
| 3 | `SpanResolver.swift:110` | Routed not first | `.routed` first event |

## Tests

`Tests/MnemoOrchestratorTests/D0059SpanResolverTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0059SpanResolver
```

**Note:** `swift test` requires macOS (AVFoundation).
