# H-0959 Evidence

| Field | Value |
|-------|-------|
| Prompt | [0959](phase2/agent-h-integration/0959.md) |
| Scenario | warm vs cold first-token bench |
| Seed | `31f4acd692ff` |
| Phase |  |
| XCTest filter | IntegrationLifecycleTests / WarmColdBenchIntegrationTests |

## Transcript (offline lifecycle)

```
PROMPT: Prove "warm vs cold first-token bench" offline (seed 31f4acd692ff)
SOURCES: Tests/Fixtures/corpus/, BeatsSiriFixtures, IntegrationLifecycleTests
ANSWER: Lifecycle test WarmColdBenchIntegrationTests exercises the scenario without network mocks bypassing egress guard.
EGRESS_NONLOOPBACK: 0
```

## macOS verification (required for green)

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
export MNEMO_BUILD_DIR=.build/ci
swift test --build-path .build/ci --filter 'WarmColdBenchIntegrationTests'
mnemoctl bench
scripts/airplane-parity.sh
MNEMO_BUILD_DIR=.build/ci scripts/run-usecases.sh
```

### Static verification (Linux)
```
$ ./scripts/verify-agent-c.sh
VERIFY SHIM: swift/Xcode-beta unavailable on this host
Static checks:
  [ok] MnemoConfig.validateInvariant
  [ok] exit code contract
  [ok] StructuredLog.swift
  [ok] LoopbackAudit rivet/smfs
  [ok] StackHealth reasons
  [ok] scripts/ci.sh
  [ok] CI workflow
VERIFY SHIM OK (static; run on macOS for full suite)

$ grep -c usecases scripts/usecases.tsv (non-comment rows)
105

$ mnemo.toml [sla]
[sla]
first_token_ms = 1500                   # P95 under background load
sources_render_ms = 1000                # source cards sub-second

[ui]

$ TimelineBuilder duplicate-method check (must be 1 each)
citationIntegritySupported: 1
unsupportedAnswerEvents: 1

$ IntegrationLifecycleTests.swift present
ok

$ egress count (this host, no live stack)
EGRESS_NONLOOPBACK: 0
```

## SLA reference

- first_token_ms: 1500 (P95 under background load)
- sources_render_ms: 1000

## Status

- [x] Evidence captured (static + test mapping)
- [ ] Full macOS harness green (requires Xcode-beta host)
- **Do not commit** — advance to H-0960
