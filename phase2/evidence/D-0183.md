# D-0183 QueryDecomposer: char-span fuzzing

**Seed:** `a5d20d3e83e3`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `QueryDecomposer.swift:30` | Out-of-range span indices crashed resolver | Bounds-checked span fuzz path |
| 2 | `CharSpan.swift:40` | Single-token false positives | Minimum token length guard |
| 3 | `QueryDecomposer.swift:55` | Non-deterministic fuzz ordering | Seed-stable fuzz via Phase2RNG |

## Tests

`Tests/MnemoOrchestratorTests/D0183QueryDecomposerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0183QueryDecomposer
```

**Note:** `swift test` requires macOS (AVFoundation).
