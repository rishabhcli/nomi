# D-0191 TimelineBuilder: agentic grep deadlock prevention

**Seed:** `6519a9e98efe`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:153` | Repeated hop spun forever | `isRepeatedHop` stops loop |
| 2 | `TimelineBuilder.swift:40` | Identical query re-queued | Hop deduplication set |
| 3 | `TimelineBuilder.swift:90` | Max hops unenforced | Hard cap on hop count |

## Tests

`Tests/MnemoOrchestratorTests/D0191TimelineBuilderTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0191TimelineBuilder
```

**Note:** `swift test` requires macOS (AVFoundation).
