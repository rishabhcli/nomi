# D-0263 SpanResolver: char-span fuzzing

**Seed:** `e232d2b3f79c`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `CharSpan.swift` | Out-of-range offsets accepted | Bounds check on resolve |
| 2 | `SpanResolver.swift` | Whitespace collapse mismatch | Word-sequence alignment |
| 3 | `Highlight.swift:59` | Empty query terms panic | Guard empty terms |

## Tests

`Tests/MnemoOrchestratorTests/D0263SpanResolverTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0263SpanResolver
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
