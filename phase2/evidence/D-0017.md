# D-0017 IngestGate: AsyncStream cancellation

**Seed:** `402ba36fd5c9`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `IngestGate.swift:61` | Poll loop ignored `Task.isCancelled` | Cancellation check each iteration |
| 2 | `IngestGate.swift:63` | `Task.sleep` errors swallowed with `try?` | `do/catch` returns `false` on cancellation |
| 3 | N/A | No cancellable AsyncStream wrapper | `searchableStream` with `onTermination` cancel |

## Tests

`Tests/MnemoOrchestratorTests/D0017IngestGateTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0017IngestGate
```

**Note:** `swift test` requires macOS (AVFoundation).
