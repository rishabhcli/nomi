# D-0161 SpanResolver: property-based invariants

**Seed:** `726b35a092ab`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `SpanResolver.swift:60` | Invariant not exposed for property tests | Added `propertyInvariantsHold` |
| 2 | `SpanResolver.swift:1` | Duplicate audit stubs caused drift | Consolidated hardening hooks |
| 3 | `SpanResolver.swift:90` | Edge-case empty input unhandled | Empty-input invariant enforced |

## Tests

`Tests/MnemoOrchestratorTests/D0161SpanResolverTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0161SpanResolver
```

**Note:** `swift test` requires macOS (AVFoundation).
