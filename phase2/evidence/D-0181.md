# D-0181 NotchReducer: property-based invariants

**Seed:** `240bf4309de7`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `NotchReducer.swift:60` | Invariant not exposed for property tests | Added `propertyInvariantsHold` |
| 2 | `NotchReducer.swift:1` | Duplicate audit stubs caused drift | Consolidated hardening hooks |
| 3 | `NotchReducer.swift:90` | Edge-case empty input unhandled | Empty-input invariant enforced |

## Tests

`Tests/MnemoOrchestratorTests/D0181NotchReducerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0181NotchReducer
```

**Note:** `swift test` requires macOS (AVFoundation).
