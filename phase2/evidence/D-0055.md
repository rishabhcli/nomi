# D-0055 EvidenceGathering: egress guard host parsing

**Seed:** `b208ce604b7a`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `EgressGuard.swift:72` | Prefix-only 127. check spoofed | Four-octet 127.0.0.0/8 validation |
| 2 | `EvidenceGathering.swift:25` | localhost alias missed | localhost/::1 accepted |
| 3 | `EvidenceGathering.swift:50` | evil.com via 127. prefix accepted | Strict octet parsing |

## Tests

`Tests/MnemoOrchestratorTests/D0055EvidenceGatheringTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0055EvidenceGathering
```

**Note:** `swift test` requires macOS (AVFoundation).
