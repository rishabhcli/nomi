# D-0166 ContextAssembler: token budget adversarial trim

**Seed:** `cd2a10499ce2`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContextAssembler.swift:90` | Adversarial padding exceeded budget | `trimAdversarial` caps tokens |
| 2 | `ContextAssembler.swift:96` | Preamble ignored in budget | Preamble fraction reserved |
| 3 | `ContextAssembler.swift:110` | Zero-budget returned non-empty | Empty guard on budget=0 |

## Tests

`Tests/MnemoOrchestratorTests/D0166ContextAssemblerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0166ContextAssembler
```

**Note:** `swift test` requires macOS (AVFoundation).
