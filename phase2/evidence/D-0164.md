# D-0164 KeywordBackstop: offline refusal paths

**Seed:** `a69a7864e509`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `KeywordBackstop.swift:20` | Silent empty screen on offline refusal | Renderable `offlineRefusalEvents` |
| 2 | `QueryService.swift:40` | Chat-only hits treated as primary | Primary evidence gate |
| 3 | `KeywordBackstop.swift:80` | Terminal state missing for refusal | `.state(.empty)` wired |

## Tests

`Tests/MnemoOrchestratorTests/D0164KeywordBackstopTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0164KeywordBackstop
```

**Note:** `swift test` requires macOS (AVFoundation).
