# D-0008 SpanResolver: citation verifier false-positive elimination

**Seed:** `3dcedb5dff2a`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `SpanResolver.swift:107-109` | Char spans set on any word match — single-token false positives | `spanConfidence >= 0.6` gate |
| 2 | `SpanResolver.swift:90-119` | No confidence metric for span quality | `spanConfidence(chunk:matchedText:)` |
| 3 | `CharSpan.swift:66` | Single-word chunks resolved in documents | Minimum 2 tokens required (coordinated) |

## Tests

`Tests/MnemoOrchestratorTests/D0008SpanResolverTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0008SpanResolver
```

**Note:** `swift test` requires macOS (AVFoundation).
