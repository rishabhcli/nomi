# D-0054 RouterEscalator: answer cache key collision

**Seed:** `0ff7d7bdd6cf`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AnswerCache.swift:76` | Container omitted from key | `container::query` key |
| 2 | `RouterEscalator.swift:40` | Case-sensitive key collisions | Lowercased trimmed key |
| 3 | `RouterEscalator.swift:65` | Same query different evidence same key | Evidence hash in key |

## Tests

`Tests/MnemoOrchestratorTests/D0054RouterEscalatorTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0054RouterEscalator
```

**Note:** `swift test` requires macOS (AVFoundation).
