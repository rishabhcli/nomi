# D-0208 EvidenceGathering: citation verifier false-positive elimination

**Seed:** `b4b478a4ca20`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `CitationVerifier.swift:147` | Punctuation fragments auto-supported | Alphanumeric trivial check |
| 2 | `EvidenceGathering.swift:60` | False positive on short claims | Minimum claim length |
| 3 | `EvidenceGathering.swift:85` | Stripped citations still counted | Strip-before-verify |

## Tests

`Tests/MnemoOrchestratorTests/D0208EvidenceGatheringTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0208EvidenceGathering
```

**Note:** `swift test` requires macOS (AVFoundation).
