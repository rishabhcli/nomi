# D-0162 CharSpan: concurrency stress under WorkScheduler

**Seed:** `ea07f39cd695`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `CharSpan.swift:70` | Interactive token leak under concurrent stress | WorkScheduler token lifecycle hardened |
| 2 | `WorkScheduler.swift:93` | Background chunks not abandoned on yield | Verified `runBackgroundChunked` abandons |
| 3 | `CharSpan.swift:45` | Race on shared mutable state | Actor-isolated access paths |

## Tests

`Tests/MnemoOrchestratorTests/D0162CharSpanTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0162CharSpan
```

**Note:** `swift test` requires macOS (AVFoundation).
