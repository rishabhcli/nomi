# D-0073 Consolidation: profile preamble staleness

**Seed:** `56bc0f198674`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContextAssembler.swift:11` | Stale facts in preamble | `filterStaleFacts` |
| 2 | `Consolidation.swift:55` | Forgotten memories in profile | Active-only filter |
| 3 | `Profile.swift:77` | Dedupe missed normalized variants | Normalize before dedupe |

## Tests

`Tests/MnemoOrchestratorTests/D0073ConsolidationTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0073Consolidation
```

**Note:** `swift test` requires macOS (AVFoundation).
