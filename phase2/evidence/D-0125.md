# D-0125 LLMSynthesizer: cache poisoning resistance

**Seed:** `70099011bf40`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `LLMSynthesizer.swift:25` | Remote URL in wire result accepted | Poisoned path/host stripped |
| 2 | `EngineClient.swift:50` | Non-loopback backing store leaked | Loopback-only validation |
| 3 | `LLMSynthesizer.swift:70` | Cache key included poisoned host | Sanitized cache key |

## Tests

`Tests/MnemoOrchestratorTests/D0125LLMSynthesizerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0125LLMSynthesizer
```

**Note:** `swift test` requires macOS (AVFoundation).
