# D-0004 EvidenceGathering: offline refusal paths

**Seed:** `559d84de90de`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `EvidenceGathering.swift:63` | Agentic grep errors swallowed via `try?` — silent empty evidence offline | `do/catch` records `"Agentic search could not complete offline"` step |
| 2 | `EvidenceGathering.swift:39` | Document search failures silent (`try?`) | `do/catch` records `"Document search unavailable offline"` |
| 3 | `EvidenceGathering.swift:82` | Query echo used `.contains` — junk queries could self-ground or skip valid recall | `isQueryEcho(_:query:)` with equality/prefix checks |
| 4 | `EvidenceGathering.swift:73-94` | Chat recall could be sole evidence (offline refusal violation) | `hasPrimaryEvidence` strips chat-only hits with reasoning step |

## Tests

`Tests/MnemoOrchestratorTests/D0004EvidenceGatheringTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0004EvidenceGathering
```

**Note:** `swift test` requires macOS (AVFoundation). Linux CI output:

```
error: no such module 'AVFoundation' (LocalExtractor.swift:1)
```
