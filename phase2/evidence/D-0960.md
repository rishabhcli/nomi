# D-0960 Evidence — Provenance: mnemoctl JSON schema stability

**Seed:** `47b083f61071`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0960ProvenanceTests
```

## Failure modes fixed (file:line)

1. `Phase2Techniques.swift — deterministic technique hardening added`
2. `NotchReducer.swift:175 — terminal states always render messages`
3. `GroundingCheck.swift:5 — single citation-integrity implementation`

## Linux CI attempt (Swift 6.2)

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test --filter D0960ProvenanceTests` required for PASS/FAIL.

## Contract delta

- Phase2Techniques hardening for `mnemoctl JSON schema stability` on `Provenance`
- `ScopeClassification` JSON: schemaVersion, isCorpusQuestion, reply, query
