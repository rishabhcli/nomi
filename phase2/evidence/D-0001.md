# D-0001 Evidence — QueryService property-based invariants

**Seed:** `0a95dece6bd1`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0001QueryServicePropertyTests
```

## Failure modes fixed (file:line)

1. `QueryService.swift:306` — called missing `Coverage.emptyEvidenceEvents()` → added in `Coverage.swift`
2. `QueryService.swift:301` — called missing `CorpusSuggester.emptyEvidenceEvents(nearest:)` → added
3. `QueryService.swift:446-449` — catch mapped all errors to `.engineUnreachable` → typed mapping via `lifecycleRetryEvents(for:)`
4. `QueryService.swift:232-234` — ambiguity `.reasoning` emitted before `.routed` then wiped → reordered + NotchReducer append
5. `EvidenceGathering.swift:55` — escalation replaced decomposed hits → merge with dedupe
6. `QueryService.swift:465-467` — relative engine paths skipped mount join → fixed `absolutePath`

## Linux CI attempt (Swift 6.2)

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.

## Contract delta

- `Coverage.emptyEvidenceEvents()` → `[.state(.empty(nearest: [])), .reasoning([...])]`
- `CorpusSuggester.emptyEvidenceEvents(nearest:)` → terminal + optional suggestions
