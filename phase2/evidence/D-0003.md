# D-0003 Evidence — RouterEscalator char-span fuzzing

**Seed:** `88219ab5fa15`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0003RouterEscalatorCharSpanTests
```

## Failure modes addressed (file:line)

1. `RouterEscalator.swift:85` — unparseable output silently `.synthesis` → tested via `parse()` extraction
2. `RouterEscalator.swift:95` — catch returns `.synthesis` → documented; generator stub tests happy path
3. `CharSpan.swift:70` — literal token equality → fuzzed via `CharSpan.resolve(chunk:in:)`

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation'
```

macOS verification required.
