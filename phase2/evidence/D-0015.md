# D-0015 OllamaClient: egress guard host parsing

**Seed:** `63f70d070176`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `OllamaClient.swift:96-100` | No loopback validation on `baseURL` init | `preconditionFailure` + `EgressGuard.isLoopbackHost` |
| 2 | `OllamaClient.swift:115` | Stream did not re-check host at request time | `parseLoopbackHost` guard before HTTP call |
| 3 | N/A | No typed error for non-loopback model host | `OllamaError.nonLoopbackHost` |

## Tests

`Tests/MnemoOrchestratorTests/D0015OllamaClientTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0015OllamaClient
```

**Note:** `swift test` requires macOS (AVFoundation).
