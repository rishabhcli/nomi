# D-0063 LLMHopPlanner: char-span fuzzing

**Seed:** `04fdd7851326`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `LLMHopPlanner.swift:30` | Out-of-range span indices crashed resolver | Bounds-checked span fuzz path |
| 2 | `CharSpan.swift:40` | Single-token false positives | Minimum token length guard |
| 3 | `LLMHopPlanner.swift:55` | Non-deterministic fuzz ordering | Seed-stable fuzz via Phase2RNG |

## Tests

`Tests/MnemoOrchestratorTests/D0063LLMHopPlannerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0063LLMHopPlanner
```

**Note:** `swift test` requires macOS (AVFoundation).
