# D-0023 LLMSynthesizer: char-span fuzzing

**Seed:** `5ad937f8aed4`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `LLMSynthesizer.swift:30` | Out-of-range span indices crashed resolver | Bounds-checked span fuzz path |
| 2 | `CharSpan.swift:40` | Single-token false positives | Minimum token length guard |
| 3 | `LLMSynthesizer.swift:55` | Non-deterministic fuzz ordering | Seed-stable fuzz via Phase2RNG |

## Tests

`Tests/MnemoOrchestratorTests/D0023LLMSynthesizerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0023LLMSynthesizer
```

**Note:** `swift test` requires macOS (AVFoundation).
