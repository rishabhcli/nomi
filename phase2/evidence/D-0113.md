# D-0113 KeywordBackstop: profile preamble staleness

**Seed:** `e731cbfe8007`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContextAssembler.swift:11` | Stale facts in preamble | `filterStaleFacts` |
| 2 | `KeywordBackstop.swift:55` | Forgotten memories in profile | Active-only filter |
| 3 | `Profile.swift:77` | Dedupe missed normalized variants | Normalize before dedupe |

## Tests

`Tests/MnemoOrchestratorTests/D0113KeywordBackstopTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0113KeywordBackstop
```

**Note:** `swift test` requires macOS (AVFoundation).
