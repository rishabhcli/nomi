# D-0171 SyncEngine: agentic grep deadlock prevention

**Seed:** `d359405159bd`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:153` | Repeated hop spun forever | `isRepeatedHop` stops loop |
| 2 | `SyncEngine.swift:40` | Identical query re-queued | Hop deduplication set |
| 3 | `SyncEngine.swift:90` | Max hops unenforced | Hard cap on hop count |

## Tests

`Tests/MnemoOrchestratorTests/D0171SyncEngineTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0171SyncEngine
```

**Note:** `swift test` requires macOS (AVFoundation).
