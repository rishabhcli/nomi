# D-0111 CharSpan: agentic grep deadlock prevention

**Seed:** `c05dbd5ed0f7`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:153` | Repeated hop spun forever | `isRepeatedHop` stops loop |
| 2 | `CharSpan.swift:40` | Identical query re-queued | Hop deduplication set |
| 3 | `CharSpan.swift:90` | Max hops unenforced | Hard cap on hop count |

## Tests

`Tests/MnemoOrchestratorTests/D0111CharSpanTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0111CharSpan
```

**Note:** `swift test` requires macOS (AVFoundation).
