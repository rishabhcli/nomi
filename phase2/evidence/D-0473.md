# D-0473 Prompt: profile preamble staleness

**Seed:** `c52d999811c0`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContextAssembler.swift:134` | Exact-match stale detection | Normalized comparison |
| 2 | `Profile.swift:77` | normalize not exposed for tests | normalizedFact public |
| 3 | `ContextAssembler.swift:95` | Stale facts leaked into preamble | MemoryFactFilter.filterProfile |

## Tests

`Tests/MnemoOrchestratorTests/D0473PromptTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0473Prompt
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
