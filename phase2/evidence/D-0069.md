# D-0069 SyncEngine: memory supersession race conditions

**Seed:** `6020cab37bd8`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `CharSpan.swift:88` | Version omitted from supersession key | `supersessionKey` includes version |
| 2 | `SyncEngine.swift:45` | Concurrent supersede double-wrote | Idempotent supersession guard |
| 3 | `MemoryDynamics.swift:248` | Race on latest flag | Latest-only supersession filter |

## Tests

`Tests/MnemoOrchestratorTests/D0069SyncEngineTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0069SyncEngine
```

**Note:** `swift test` requires macOS (AVFoundation).
