# D-0335 QueryRewriter: egress guard host parsing

**Seed:** `167c900e4a3a`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `EgressGuard.swift:69` | hasPrefix 127. spoofable | Four-octet validation |
| 2 | `ActionExtractor.swift:67` | External URLs in actions | actionHostIsLoopback filter |
| 3 | `EgressGuard.swift:88` | Empty host counted as egress | Nil host skipped in canInit |

## Tests

`Tests/MnemoOrchestratorTests/D0335QueryRewriterTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0335QueryRewriter
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
