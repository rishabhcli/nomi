# D-0104 Router: offline refusal paths

**Seed:** `2a26baad0835`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `Router.swift:20` | Silent empty screen on offline refusal | Renderable `offlineRefusalEvents` |
| 2 | `QueryService.swift:40` | Chat-only hits treated as primary | Primary evidence gate |
| 3 | `Router.swift:80` | Terminal state missing for refusal | `.state(.empty)` wired |

## Tests

`Tests/MnemoOrchestratorTests/D0104RouterTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0104Router
```

**Note:** `swift test` requires macOS (AVFoundation).
