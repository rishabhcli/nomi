# D-0031 ScopeClassifier: agentic grep deadlock prevention

**Seed:** `23ea5484db73`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:153` | Repeated hop spun forever | `isRepeatedHop` stops loop |
| 2 | `ScopeClassifier.swift:40` | Identical query re-queued | Hop deduplication set |
| 3 | `ScopeClassifier.swift:90` | Max hops unenforced | Hard cap on hop count |

## Tests

`Tests/MnemoOrchestratorTests/D0031ScopeClassifierTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0031ScopeClassifier
```

**Note:** `swift test` requires macOS (AVFoundation).
