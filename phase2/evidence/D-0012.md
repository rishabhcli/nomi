# D-0012 LLMHopPlanner: numeric synthesis distractor immunity

**Seed:** `ab4928f1fedb`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `LLMHopPlanner.swift:102` | Numeric-only hop queries accepted as semantic search | `isNumericDistractor` rejects when question has no digits |
| 2 | `LLMHopPlanner.swift:90-105` | Repeated hops not enforced in `parse` | `isRepeatedHop` + `priorHops` parameter |
| 3 | `LLMHopPlanner.swift:150` | Parse ignored question context for distractors | `parse(raw, question:, priorHops:)` |

## Tests

`Tests/MnemoOrchestratorTests/D0012LLMHopPlannerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0012LLMHopPlanner
```

**Note:** `swift test` requires macOS (AVFoundation).
