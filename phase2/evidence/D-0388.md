# D-0388 ScopeClassifier: citation verifier false-positive elimination

**Seed:** `8cd5f460b829`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `CitationVerifier.swift` | Short tokens false-positive | min token length 4 |
| 2 | `ContextAssembler.swift:32` | Empty claim marked supported | Empty claim returns true |
| 3 | `Verification.swift` | Citation strip incomplete | Bracket pattern match |

## Tests

`Tests/MnemoOrchestratorTests/D0388ScopeClassifierTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0388ScopeClassifier
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
