# D-0080 QueryRewriter: mnemoctl JSON schema stability

**Seed:** `8bcf6c3ac633`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `mnemoctl/main.swift:600` | JSON schema unstable | `schemaVersion: 1` |
| 2 | `QueryRewriter.swift:30` | Keys unsorted in export | Sorted JSON keys |
| 3 | `QueryRewriter.swift:50` | Forgotten entries in export | Active-only filter |

## Tests

`Tests/MnemoOrchestratorTests/D0080QueryRewriterTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0080QueryRewriter
```

**Note:** `swift test` requires macOS (AVFoundation).
