# D-0074 LLMSynthesizer: answer cache key collision

**Seed:** `54824a63987d`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AnswerCache.swift:76` | Container omitted from key | `container::query` key |
| 2 | `LLMSynthesizer.swift:40` | Case-sensitive key collisions | Lowercased trimmed key |
| 3 | `LLMSynthesizer.swift:65` | Same query different evidence same key | Evidence hash in key |

## Tests

`Tests/MnemoOrchestratorTests/D0074LLMSynthesizerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0074LLMSynthesizer
```

**Note:** `swift test` requires macOS (AVFoundation).
