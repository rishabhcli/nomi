# D-0201 Preferences: property-based invariants

**Seed:** `bc5f6b15be43`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `Preferences.swift:60` | Invariant not exposed for property tests | Added `propertyInvariantsHold` |
| 2 | `Preferences.swift:1` | Duplicate audit stubs caused drift | Consolidated hardening hooks |
| 3 | `Preferences.swift:90` | Edge-case empty input unhandled | Empty-input invariant enforced |

## Tests

`Tests/MnemoOrchestratorTests/D0201PreferencesTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0201Preferences
```

**Note:** `swift test` requires macOS (AVFoundation).
