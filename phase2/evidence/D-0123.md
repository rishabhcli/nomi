# D-0123 ConflictDetector: char-span fuzzing

**Seed:** `9e14a3ee075c`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ConflictDetector.swift:30` | Out-of-range span indices crashed resolver | Bounds-checked span fuzz path |
| 2 | `CharSpan.swift:40` | Single-token false positives | Minimum token length guard |
| 3 | `ConflictDetector.swift:55` | Non-deterministic fuzz ordering | Seed-stable fuzz via Phase2RNG |

## Tests

`Tests/MnemoOrchestratorTests/D0123ConflictDetectorTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0123ConflictDetector
```

**Note:** `swift test` requires macOS (AVFoundation).
