# D-0121 ContentHash: property-based invariants

**Seed:** `4740317b85fa`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContentHash.swift:60` | Invariant not exposed for property tests | Added `propertyInvariantsHold` |
| 2 | `ContentHash.swift:1` | Duplicate audit stubs caused drift | Consolidated hardening hooks |
| 3 | `ContentHash.swift:90` | Edge-case empty input unhandled | Empty-input invariant enforced |

## Tests

`Tests/MnemoOrchestratorTests/D0121ContentHashTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0121ContentHash
```

**Note:** `swift test` requires macOS (AVFoundation).
