# D-0224 MemoryDynamics: offline refusal paths

**Seed:** `a0233459673a`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `MemoryDynamics.swift:20` | Silent empty screen on offline refusal | Renderable `offlineRefusalEvents` |
| 2 | `QueryService.swift:40` | Chat-only hits treated as primary | Primary evidence gate |
| 3 | `MemoryDynamics.swift:80` | Terminal state missing for refusal | `.state(.empty)` wired |

## Tests

`Tests/MnemoOrchestratorTests/D0224MemoryDynamicsTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0224MemoryDynamics
```

**Note:** `swift test` requires macOS (AVFoundation).
