# D-0242 TimelineBuilder: concurrency stress under WorkScheduler

**Seed:** `e8d2f10e1ef6`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `TimelineBuilder.swift:70` | Interactive token leak under concurrent stress | WorkScheduler token lifecycle hardened |
| 2 | `WorkScheduler.swift:93` | Background chunks not abandoned on yield | Verified `runBackgroundChunked` abandons |
| 3 | `TimelineBuilder.swift:45` | Race on shared mutable state | Actor-isolated access paths |

## Tests

`Tests/MnemoOrchestratorTests/D0242TimelineBuilderTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0242TimelineBuilder
```

**Note:** `swift test` requires macOS (AVFoundation).
