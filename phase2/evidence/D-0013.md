# D-0013 ContextAssembler: profile preamble staleness

**Seed:** `bdafd1517fcd`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContextAssembler.swift:94` | Stale profile facts included after memory supersession | `activeFactTexts` + `MemoryFactFilter.filterProfile` |
| 2 | `ContextAssembler.swift:110-125` | No staleness detection API | `staleFacts(in:activeTexts:)` |
| 3 | `ContextAssembler.swift:125` | Preamble had no generation stamp for cache invalidation | `profile-stamp` comment in preamble |

## Tests

`Tests/MnemoOrchestratorTests/D0013ContextAssemblerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0013ContextAssembler
```

**Note:** `swift test` requires macOS (AVFoundation).
