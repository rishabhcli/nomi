# D-0144 Provenance: offline refusal paths

**Seed:** `77aedc6b19e4`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `Provenance.swift:20` | Silent empty screen on offline refusal | Renderable `offlineRefusalEvents` |
| 2 | `QueryService.swift:40` | Chat-only hits treated as primary | Primary evidence gate |
| 3 | `Provenance.swift:80` | Terminal state missing for refusal | `.state(.empty)` wired |

## Tests

`Tests/MnemoOrchestratorTests/D0144ProvenanceTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0144Provenance
```

**Note:** `swift test` requires macOS (AVFoundation).
