# D-0006 EngineIntegration: token budget adversarial trim

**Seed:** `4f5ad49c2ccf`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | N/A (missing) | No adversarial trim — low-similarity padding could crowd out evidence | `ContainerCatalog.trimEvidenceAdversarial` |
| 2 | `EngineIntegration.swift:170` | First `isRelevant` chunk chosen, not highest score | `max(by: score)` selection |
| 3 | `EngineIntegration.swift:221` | `fileURL` returned non-loopback presigned URLs | Loopback host guard on decoded URL |

## Tests

`Tests/MnemoOrchestratorTests/D0006EngineIntegrationTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0006EngineIntegration
```

**Note:** `swift test` requires macOS (AVFoundation).
