# D-0081 QueryDecomposer: property-based invariants

**Seed:** `d1a7358b21f5`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `QueryDecomposer.swift:60` | Invariant not exposed for property tests | Added `propertyInvariantsHold` |
| 2 | `QueryDecomposer.swift:1` | Duplicate audit stubs caused drift | Consolidated hardening hooks |
| 3 | `QueryDecomposer.swift:90` | Edge-case empty input unhandled | Empty-input invariant enforced |

## Tests

`Tests/MnemoOrchestratorTests/D0081QueryDecomposerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0081QueryDecomposer
```

**Note:** `swift test` requires macOS (AVFoundation).
