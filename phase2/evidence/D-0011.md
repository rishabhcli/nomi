# D-0011 KeywordBackstop: agentic grep deadlock prevention

**Seed:** `efe7789f5606`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `KeywordBackstop.swift:137` | Unbounded directory scan could block interactive path | `maxFilesScanned = 24` |
| 2 | `KeywordBackstop.swift:250` | Unbounded term grep on numeric/title-match rescue | `maxRescueTerms = 6` cap |
| 3 | `KeywordBackstop.swift:101-106` | Chat recall counted as document coverage | `uncovered` already filters `chatRecallTitle` — test locks behavior |

## Tests

`Tests/MnemoOrchestratorTests/D0011KeywordBackstopTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0011KeywordBackstop
```

**Note:** `swift test` requires macOS (AVFoundation).
