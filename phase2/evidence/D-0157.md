# D-0157 EvidenceGathering: AsyncStream cancellation

**Seed:** `2a61c75139b6`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `EvidenceGathering.swift:60` | Stream continued after cancel | Cancellation check in loop |
| 2 | `IngestGate.swift:22` | Wait ignored Task.isCancelled | Honours cancellation |
| 3 | `EvidenceGathering.swift:90` | Orphaned continuation on cancel | Clean termination |

## Tests

`Tests/MnemoOrchestratorTests/D0157EvidenceGatheringTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0157EvidenceGathering
```

**Note:** `swift test` requires macOS (AVFoundation).
