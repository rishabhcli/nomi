# D-0066 OllamaClient: token budget adversarial trim

**Seed:** `fedb1e0269de`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `OllamaClient.swift:90` | Adversarial padding exceeded budget | `trimAdversarial` caps tokens |
| 2 | `ContextAssembler.swift:96` | Preamble ignored in budget | Preamble fraction reserved |
| 3 | `OllamaClient.swift:110` | Zero-budget returned non-empty | Empty guard on budget=0 |

## Tests

`Tests/MnemoOrchestratorTests/D0066OllamaClientTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0066OllamaClient
```

**Note:** `swift test` requires macOS (AVFoundation).
