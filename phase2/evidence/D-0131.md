# D-0131 QueryRewriter: agentic grep deadlock prevention

**Seed:** `dd9287926842`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:153` | Repeated hop spun forever | `isRepeatedHop` stops loop |
| 2 | `QueryRewriter.swift:40` | Identical query re-queued | Hop deduplication set |
| 3 | `QueryRewriter.swift:90` | Max hops unenforced | Hard cap on hop count |

## Tests

`Tests/MnemoOrchestratorTests/D0131QueryRewriterTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0131QueryRewriter
```

**Note:** `swift test` requires macOS (AVFoundation).
