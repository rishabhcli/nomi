# D-0447 ResponseStyle: router escalation boundaries

**Seed:** `e5deaac7e8b5`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `RouterEscalator.swift` | Unbounded escalation depth | maxEscalation cap |
| 2 | `Coverage.swift:57` | Threshold could go below zero | max(0.1, threshold*0.5) |
| 3 | `Router.swift` | Effort never downgraded | Boundary at multihop |

## Tests

`Tests/MnemoOrchestratorTests/D0447ResponseStyleTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0447ResponseStyle
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
