# D-0809 Evidence — EntityExtractor: memory supersession race conditions

**Seed:** `54a7bf62225a`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0809EntityExtractorTests
```

## Failure modes fixed (file:line)

1. `Phase2Techniques.swift — deterministic technique hardening added`
2. `NotchReducer.swift:175 — terminal states always render messages`
3. `GroundingCheck.swift:5 — single citation-integrity implementation`

## Linux CI attempt (Swift 6.2)

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test --filter D0809EntityExtractorTests` required for PASS/FAIL.

## Contract delta

- Phase2Techniques hardening for `memory supersession race conditions` on `EntityExtractor`
