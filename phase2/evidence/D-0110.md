# D-0110 SpanResolver: ingest gate timing proofs

**Seed:** `92cb6fb3a89f`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `IngestGate.swift:30` | Wait loop ignored cancellation | Cancellation-aware wait |
| 2 | `SpanResolver.swift:50` | Timeout returned true spuriously | Honest timeout=false |
| 3 | `SpanResolver.swift:75` | Stream never terminated | Terminal `.finished` event |

## Tests

`Tests/MnemoOrchestratorTests/D0110SpanResolverTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0110SpanResolver
```

**Note:** `swift test` requires macOS (AVFoundation).
