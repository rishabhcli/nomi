# D-0226 Consolidation: token budget adversarial trim

**Seed:** `ca76ed52a7cf`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `Consolidation.swift:90` | Adversarial padding exceeded budget | `trimAdversarial` caps tokens |
| 2 | `ContextAssembler.swift:96` | Preamble ignored in budget | Preamble fraction reserved |
| 3 | `Consolidation.swift:110` | Zero-budget returned non-empty | Empty guard on budget=0 |

## Tests

`Tests/MnemoOrchestratorTests/D0226ConsolidationTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0226Consolidation
```

**Note:** `swift test` requires macOS (AVFoundation).
