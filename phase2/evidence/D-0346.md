# D-0346 FollowUp: token budget adversarial trim

**Seed:** `5f10c462caec`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContextAssembler.swift:105` | Oversized hit skipped rest | Continue scanning smaller hits |
| 2 | `TokenEstimate.swift:20` | Zero-length counted as 1 | max(1, ...) guard |
| 3 | `KeywordBackstop.swift` | Adversarial long query blew budget | Trim to budget |

## Tests

`Tests/MnemoOrchestratorTests/D0346FollowUpTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0346FollowUp
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
