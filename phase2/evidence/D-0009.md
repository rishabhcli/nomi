# D-0009 CharSpan: memory supersession race conditions

**Seed:** `637aeadc46dd`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `CharSpan.swift:68-77` | First match returned — superseded shorter span won race | Prefer longest matching span |
| 2 | `CharSpan.swift:66` | Single-token matches caused supersession collisions | Require `chunkWords.count >= 2` |
| 3 | N/A | No version-aware span identity | `supersessionKey(docId:version:range:)` |

## Tests

`Tests/MnemoOrchestratorTests/D0009CharSpanTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0009CharSpan
```

**Note:** `swift test` requires macOS (AVFoundation).
