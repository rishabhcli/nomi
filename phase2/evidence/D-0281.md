# D-0281 EgressGuard: property-based invariants

**Seed:** `2c9e25e760d6`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `QueryService.swift` | Non-deterministic routing | Phase2RNG property tests |
| 2 | `Router.swift` | Effort escalation unbounded | maxHops cap invariant |
| 3 | `Coverage.swift:52` | Weak threshold not monotonic | isWeak consistent |

## Tests

`Tests/MnemoOrchestratorTests/D0281EgressGuardTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0281EgressGuard
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
