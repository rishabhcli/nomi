# D-0186 AnswerCache: token budget adversarial trim

**Seed:** `d3631502ea84`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AnswerCache.swift:90` | Adversarial padding exceeded budget | `trimAdversarial` caps tokens |
| 2 | `ContextAssembler.swift:96` | Preamble ignored in budget | Preamble fraction reserved |
| 3 | `AnswerCache.swift:110` | Zero-budget returned non-empty | Empty guard on budget=0 |

## Tests

`Tests/MnemoOrchestratorTests/D0186AnswerCacheTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0186AnswerCache
```

**Note:** `swift test` requires macOS (AVFoundation).
