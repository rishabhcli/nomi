# D-1000 Evidence — ScopeClassifier: mnemoctl JSON schema stability

**Seed:** `ce11aa17bf7c`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D1000ScopeClassifierTests
```

## Failure modes fixed (file:line)

1. `ScopeClassifier.swift:89` — `classify()` missing; mnemoctl called non-existent API → added `ScopeClassification`
2. `mnemoctl/main.swift:575` — printed `intent=` text → stable JSON via `jsonData()`
3. `Phase2Techniques.swift:168` — `ScopeClassification.schemaVersion` contract for mnemoctl
4. `ScopeClassifier.swift:80` — chit-chat gate had no machine-readable output for headless probes
5. `NotchReducer.swift:175` — terminal messages remain exhaustive for scope short-circuit paths

## Linux CI attempt (Swift 6.2)

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test --filter D1000ScopeClassifierTests` required for PASS/FAIL.

## Contract delta

- `ScopeClassification` JSON: `schemaVersion`, `query`, `isCorpusQuestion`, `reply`
- `mnemoctl scope-classify <query>` prints sorted JSON (not `intent=`)
