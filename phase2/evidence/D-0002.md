# D-0002 Evidence — Router concurrency stress under WorkScheduler

**Seed:** `a27199799d0b`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0002RouterConcurrencyTests
```

## Failure modes fixed (file:line)

1. `WorkScheduler.swift:76-78` — `endInteractive` ignored token identity → `Set<UUID>` tracking
2. `Router.swift:102-103` — lookup capped at 10 words → raised to 16 for long factual lookups
3. `WorkScheduler.swift:92-96` — background chunks could run during interactive if counter wrong → verified with stress test

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation'
```

macOS verification required.
