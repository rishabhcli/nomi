# D-0102 ActionExtractor: concurrency stress under WorkScheduler

**Seed:** `0bf00363c32a`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ActionExtractor.swift:70` | Interactive token leak under concurrent stress | WorkScheduler token lifecycle hardened |
| 2 | `WorkScheduler.swift:93` | Background chunks not abandoned on yield | Verified `runBackgroundChunked` abandons |
| 3 | `ActionExtractor.swift:45` | Race on shared mutable state | Actor-isolated access paths |

## Tests

`Tests/MnemoOrchestratorTests/D0102ActionExtractorTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0102ActionExtractor
```

**Note:** `swift test` requires macOS (AVFoundation).
