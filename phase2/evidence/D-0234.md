# D-0234 QueryDecomposer: answer cache key collision

**Seed:** `bcb99b19a4df`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AnswerCache.swift:76` | Container omitted from key | `container::query` key |
| 2 | `QueryDecomposer.swift:40` | Case-sensitive key collisions | Lowercased trimmed key |
| 3 | `QueryDecomposer.swift:65` | Same query different evidence same key | Evidence hash in key |

## Tests

`Tests/MnemoOrchestratorTests/D0234QueryDecomposerTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0234QueryDecomposer
```

**Note:** `swift test` requires macOS (AVFoundation).
