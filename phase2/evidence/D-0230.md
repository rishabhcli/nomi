# D-0230 EgressGuard: ingest gate timing proofs

**Seed:** `8ac5429df2fc`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `IngestGate.swift:30` | Wait loop ignored cancellation | Cancellation-aware wait |
| 2 | `EgressGuard.swift:50` | Timeout returned true spuriously | Honest timeout=false |
| 3 | `EgressGuard.swift:75` | Stream never terminated | Terminal `.finished` event |

## Tests

`Tests/MnemoOrchestratorTests/D0230EgressGuardTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0230EgressGuard
```

**Note:** `swift test` requires macOS (AVFoundation).
