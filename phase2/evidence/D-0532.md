# D-0532 Evidence — Consolidation numeric synthesis distractor immunity

**Seed:** `1ff1f3bc25f3`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0532ConsolidationTests
```

## Failure modes fixed (file:line)

1. Consolidation.swift — weak citationIntegrity delegated to GroundingCheck
2. Consolidation.swift — duplicate citationIntegritySupported redeclarations removed
3. Phase2Hardening — module technique hooks wired for deterministic offline tests

## Linux CI attempt (Swift 6.2)

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test --filter D0532ConsolidationTests` required for PASS/FAIL assertion.

## Contract delta

- `GroundingCheck` centralizes token-grounded `citationIntegritySupported`
- `Provenance.fromAnswer` no longer attributes source to unsupported sentences
- Phase2Hardening extensions provide deterministic technique hooks per module
