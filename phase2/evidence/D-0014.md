# D-0014 Prompt: answer cache key collision

**Seed:** `c3b9e894aff0`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | N/A (`AnswerCache.swift:76`) | Cache key ignored evidence fingerprint | `Prompt.answerCacheKey` includes evidence hash |
| 2 | `Prompt.swift:98` | Context blocks omitted `docId` — collision on same title/path | `id:docId` tag in context header |
| 3 | `Prompt.swift:89-100` | Same query + different evidence could share cache entry | FNV-style hash over container, query, corpus version, evidence |

## Tests

`Tests/MnemoOrchestratorTests/D0014PromptTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0014Prompt
```

**Note:** `swift test` requires macOS (AVFoundation).
