# D-0552 Evidence — Provenance numeric synthesis distractor immunity

**Seed:** `0448f27bcebf`  
**Verify command:**
```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0552ProvenanceTests
```

## Failure modes fixed (file:line)

1. Provenance.swift:20 — weak citationIntegrity only checked non-empty strip → GroundingCheck
2. Provenance.swift:69 — unsupported sentences inherited first source → fixed fromAnswer
3. Provenance.swift:48 — explain included sub-3-char verdict fragments

## Linux CI attempt (Swift 6.2)

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test --filter D0552ProvenanceTests` required for PASS/FAIL assertion.

## Contract delta

- `GroundingCheck` centralizes token-grounded `citationIntegritySupported`
- `Provenance.fromAnswer` no longer attributes source to unsupported sentences
- Phase2Hardening extensions provide deterministic technique hooks per module
