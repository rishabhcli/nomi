# D-0039 ResponseStyle: QueryEvent ordering guarantees

**Seed:** `07c9f8bc7384`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ResponseStyle.swift:80` | Sources after tokens | Sources precede tokens invariant |
| 2 | `QueryService.swift:200` | Done not last | Stream ends with `.done` |
| 3 | `ResponseStyle.swift:110` | Routed not first | `.routed` first event |

## Tests

`Tests/MnemoOrchestratorTests/D0039ResponseStyleTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0039ResponseStyle
```

**Note:** `swift test` requires macOS (AVFoundation).
