# D-0396 ResponseStyle: subprocess stderr backpressure

**Seed:** `f8245531a614`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:276` | stderr to nullDevice | Async stderr drain |
| 2 | `Subprocess.swift:276` | stdout blocked on full stderr pipe | Detached err drain |
| 3 | `ConflictDetector.swift` | No stderr handling in grep path | Uses shared Subprocess |

## Tests

`Tests/MnemoOrchestratorTests/D0396ResponseStyleTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0396ResponseStyle
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
