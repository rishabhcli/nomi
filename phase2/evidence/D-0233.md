# D-0233 QueryRewriter: profile preamble staleness

**Seed:** `ee746f7dc73b`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContextAssembler.swift:11` | Stale facts in preamble | `filterStaleFacts` |
| 2 | `QueryRewriter.swift:55` | Forgotten memories in profile | Active-only filter |
| 3 | `Profile.swift:77` | Dedupe missed normalized variants | Normalize before dedupe |

## Tests

`Tests/MnemoOrchestratorTests/D0233QueryRewriterTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0233QueryRewriter
```

**Note:** `swift test` requires macOS (AVFoundation).
