# D-0010 AgenticGrep: ingest gate timing proofs

**Seed:** `73570c437cbb`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:146` | Tight loop blocked ingest gate — no yield | `Task.yield()` + cancellation check each hop |
| 2 | `AgenticGrep.swift:161` | Repeated hop queries could deadlock grep | `isRepeatedHop` guard on semantic/literal |
| 3 | `AgenticGrep.swift:263` | Subprocess stderr discarded to nullDevice — backpressure risk on stdout path | Async stderr drain via `Task.detached` |

## Tests

`Tests/MnemoOrchestratorTests/D0010AgenticGrepTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0010AgenticGrep
```

**Note:** `swift test` requires macOS (AVFoundation).
