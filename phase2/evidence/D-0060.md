# D-0060 CharSpan: mnemoctl JSON schema stability

**Seed:** `e983c479eec0`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `mnemoctl/main.swift:600` | JSON schema unstable | `schemaVersion: 1` |
| 2 | `CharSpan.swift:30` | Keys unsorted in export | Sorted JSON keys |
| 3 | `CharSpan.swift:50` | Forgotten entries in export | Active-only filter |

## Tests

`Tests/MnemoOrchestratorTests/D0060CharSpanTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0060CharSpan
```

**Note:** `swift test` requires macOS (AVFoundation).
