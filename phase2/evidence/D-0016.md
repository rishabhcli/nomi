# D-0016 Ingestion: subprocess stderr backpressure

**Seed:** `8802ac5c49b0`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `AgenticGrep.swift:263` | stderr to `nullDevice` — large stderr could still block on some platforms | stderr drained on background task |
| 2 | `Ingestion.swift:147` | `refresh()` swallowed engine list failures silently | Returns `Bool` success indicator |
| 3 | N/A | No dedicated ingest subprocess helper | `IngestionSubprocess.capture` with stderr drain |

## Tests

`Tests/MnemoOrchestratorTests/D0016IngestionTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0016Ingestion
```

**Note:** `swift test` requires macOS (AVFoundation).
