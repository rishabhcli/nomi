# D-0259 EvidenceGathering: QueryEvent ordering guarantees

**Seed:** `0a65e9c0ce11`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `NotchReducer.swift` | reasoning wiped routed event | Append reasoning after routed |
| 2 | `QueryService.swift:232` | Ambiguity events out of order | Reordered emission |
| 3 | `EvidenceGathering.swift:55` | Escalation replaced prior hits | Merge with dedupe |

## Tests

`Tests/MnemoOrchestratorTests/D0259EvidenceGatheringTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0259EvidenceGathering
```

## Linux CI attempt

```
$ swift build --target MnemoOrchestrator
error: no such module 'AVFoundation' (LocalExtractor.swift)
```

Full macOS `swift test` required for PASS/FAIL assertion.
