# D-0019 ContentHash: QueryEvent ordering guarantees

**Seed:** `1dbf80db782c`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | `ContentHash.swift:56-59` | `sha256` accumulated entire file despite streaming comment | CryptoKit incremental `hasher.update` |
| 2 | N/A | No deterministic source ordering for QueryEvents | `queryEventOrderingKey` + `orderedForEvents` |
| 3 | `ContentHash.swift:53-60` | Large files could OOM during hash | True 1 MiB window streaming hash |

## Tests

`Tests/MnemoOrchestratorTests/D0019ContentHashTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0019ContentHash
```

**Note:** `swift test` requires macOS (AVFoundation).
