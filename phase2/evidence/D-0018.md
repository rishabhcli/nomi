# D-0018 SyncEngine: TerminalState exhaustiveness

**Seed:** `e11c4a1ab1ac`

## Failure modes fixed

| # | File:Line (before) | Issue | Fix |
|---|-------------------|-------|-----|
| 1 | N/A | No compile-time/runtime check all `TerminalState` cases render | `terminalStatesExhaustive()` + `allTerminalStates()` |
| 2 | N/A | Sync failures had no defined `QueryEvent` path | `terminalLifecycleEvents(_:)` |
| 3 | `SyncEngine.swift:114-122` | Self-heal errors not mapped to terminal output helpers | Exhaustiveness helpers on `SyncEngine` |

## Tests

`Tests/MnemoOrchestratorTests/D0018SyncEngineTests.swift`

## Verify

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --filter D0018SyncEngine
```

**Note:** `swift test` requires macOS (AVFoundation).
